# DreamTime Configuration
# Copy this file to .env and modify as needed

# =============================================================================
# Database Configuration
# =============================================================================
# SQLite (default - no external database required)
DATABASE_URL="file:./data/database/dreamtime.db"

# PostgreSQL (if you have an existing PostgreSQL server)
# DATABASE_URL="postgresql://user:password@your-postgres-host:5432/dreamtime"

# =============================================================================
# Server Configuration
# =============================================================================
PORT=80
NODE_ENV=production
CLIENT_URL=http://localhost

# =============================================================================
# Authentication (auto-generated if not provided)
# =============================================================================
# JWT secrets are automatically generated on first startup and persisted
# You can override them here if needed:
# JWT_SECRET=your-super-secret-jwt-key
# JWT_REFRESH_SECRET=your-super-secret-refresh-key
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# =============================================================================
# Push Notifications (auto-generated if not provided)
# =============================================================================
# VAPID keys are automatically generated on first startup and persisted
# You can override them here if needed:
# VAPID_PUBLIC_KEY=your-vapid-public-key
# VAPID_PRIVATE_KEY=your-vapid-private-key
VAPID_SUBJECT=mailto:admin@dreamtime.app

# =============================================================================
# Data Storage
# =============================================================================
# Directory where secrets and database are stored
# For Docker: these are mapped to container paths automatically
# For local dev: relative to project root
SECRETS_DIR=./data/secrets
